<h1>Listing companies</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Balance</th>
    <th>Balance date</th>
    <th>Pay date</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @companies.each do |company| %>
  <tr>
    <td><%= company.name %></td>
    <td><%= company.balance %></td>
    <td><%= company.balance_date %></td>
    <td><%= company.pay_date %></td>
    <td><%= link_to 'Show', admin_company_path(company) %></td>
    <td><%= link_to 'Edit', edit_admin_company_path(company) %></td>
    <td><%= link_to 'Destroy', admin_company_path(company), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Company', new_admin_company_path %>


