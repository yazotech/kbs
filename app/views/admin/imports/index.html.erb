<div class='toolbar'>
	
  <ul class='actions'>
    <li>
      <a href="#" class="button" onclick="$('#dialog').dialog('open');">
	      <span><img alt="Add" src="/images/admin/icons/add.png">导入数据</span>
	  </a>
    </li>
     <li>
      <a href="checkdate" class="button" >
	      <span><img alt="Add" src="/images/admin/icons/add.png">检查数据</span>
	  	</a>
    </li>
    <li>
      <a href="save" class="button" >
	      <span><img alt="Add" src="/images/admin/icons/add.png">发布数据</span>
	  	</a>
    </li>
    <li>
      <a href="clear" class="button">
      	<span><img alt="Add" src="/images/admin/icons/add.png">清空</span>
      </a>
    </li>
  </ul>
 
  <br class='clear' />
   <br/>
</div>

<div id="import_data">

<table id="table" spadding="0">
	<thead>
		<tr id="trhead">
			<th>日期</th>
			<th>ch</th>
			<th>dz count</th>
			<th>结算用户数</th>
			<th>client</th>
			<th>公司</th>
			<th>产品</th>
			<th>agreement</th>
			<th>单价</th>
			<th>总价格</th>
			<th>备注</th>
			<th>status</th>
		</tr>
	</thead>
	<tbody>
	<% if @balances !=nil %>
		<% @balances.each do |b| %>
		<tr id='<%= dom_id b %>'>
			<td><%= b.balance_date %></td>
			<td><%= b.channel_number %></td>
			<td><%= b.dz_count %></td>
			<td><%= b.count %></td>
			<td><%= b.client.name if b.client %></td>
			<td><%= Company.find_by_id(b.company_id).title if b.company_id %></td>
			<td><%= Product.find_by_id(b.product_id).title if b.product_id %></td>
			<td><%= b.agreement_id %></td>
			<td><%= b.price %></td>
			<td><%= b.amount %></td>
			<td><%= b.remark %></td>
			<td><%= b.status %></td>
		</tr>
		<% end %>
		<% end %>
	</tbody>
</table>
</div>
<div id="dialog" title="导入数据" class='dialog-window'>
	<p>
		<span class="ui-icon ui-icon-circle-check" style="float:left;margin:0 7px 50px 0;"></span>
		请把excel表格数据拷贝粘贴到下面文本框中。
	</p>
	<%= form_tag 'imports/do_import', :method => :get do %>
	<%= hidden_field_tag :op, 'import' %>
	<p>
		<%= text_area_tag 'import_text', nil, :cols => 60 %>
  </p>
    <%= submit_tag '导入数据' %>
  <% end %>
</div>
